{
  "workflow": "review-changes",
  "profile": "comprehensive",
  "inputs": {
    "diff_path": "diff --git a/src/parser.py b/src/parser.py\n--- a/src/parser.py\n+++ b/src/parser.py\n@@ -1,3 +1,4 @@\n+import textwrap\n import re\n \n def parse(raw):\n@@ -86,2 +87,4 @@\n def validate(raw):\n+    if raw == '':\n+        return None\n     return raw.strip()\ndiff --git a/src/cache.py b/src/cache.py\n--- a/src/cache.py\n+++ b/src/cache.py\n@@ -38,5 +38,4 @@\n def evict(self, key):\n-    self.flush(key)\n    node = self._unlink(key)\n+    self.flush(key)\n    return node\n",
    "context": "PR 42: harden parser input validation and rework cache eviction."
  },
  "fixtures": "fixtures.json",
  "gates": {},
  "checkpoints": {},
  "goldens": {
    "results": "golden-results.json",
    "journal": "golden-journal.jsonl"
  }
}
