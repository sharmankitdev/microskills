Finalize by vendoring the approved {{finalize_kind}} into the harness source-of-truth and syncing it into the runtime. The approved plan is at {{finalize_plan_ref}}; staged files are {{finalize_staging_ref}}; evaluator verdict {{finalize_verdict_ref}} with issues {{finalize_issues_ref}}. FIRST, if issues is non-empty (e.g. warn-severity findings that passed but were never fixed), present each as severity + message and AskUserQuestion (fix-and-finalize / accept-as-is / abandon): for fix-and-finalize, apply the fixes to the staged files before moving them; for abandon, stop and clean staging. THEN take {{finalize_name_source}}. Compute dest = ${workflow.inputs.harness_root}/{{finalize_dest_subdir}}/<name>. If dest already exists OR ${workflow.inputs.harness_yaml} already lists a {{finalize_kind}} named <name>, AskUserQuestion (overwrite / pick-new-name / abandon); pick-new-name re-computes dest. Move the staged files into dest ({{finalize_staged_layout}}); do NOT write a slash shim or copy into .claude/ directly — sync owns those. THEN upsert the manifest (v2 shape): read ${workflow.inputs.harness_yaml}, and under the top-level `{{finalize_kind}}s:` list add (or replace in place if already present) the entry {name: <name>, source: custom, profiles: [the profile stems under dest/profiles]}, preserving every other entry under `microskills:`/`workflows:` and the file's `version` field, and write it back. THEN run `.claude/scripts/harness-sync --apply` from the project root and parse its JSON. Exit 2 → surface the error and stop. Exit 1 (an unmanaged .claude/ path of the same name exists) → present the conflict and AskUserQuestion (overwrite / abandon); on overwrite re-run `.claude/scripts/harness-sync --apply --resolve <name>=overwrite`. Exit 0 → {{finalize_epilogue}}
