version: 1

# Compose: headless (the autonomous.yaml policy line, verbatim — parks honestly at
# loop_exhaust / a red T5 triage / ALWAYS at approve_publish) + the GitHub publish
# target (the github.yaml keyed patch, verbatim).
gate_mode: auto
nodes:
  patch:
    - id: publish
      customize: { profile: github }
