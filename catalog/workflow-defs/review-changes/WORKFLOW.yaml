version: 1
name: review-changes
description: Review a unified git diff end to end — summarize, review across correctness/security/performance, collect, adversarially verify, and synthesize a prioritized report, with an approval gate that can post it to a PR/MR.

# Caller-supplied inputs (referenced as ${workflow.inputs.<name>}).
inputs:
  diff_path:
    type: string
    required: true
    materialize: file
    description: >-
      The unified git diff to review, passed BY REFERENCE. `materialize: file` lets the
      dispatcher accept any of three shapes — a raw diff string, a single diff file (e.g.
      `git diff main > /tmp/x.diff`), or a directory of diff files — and normalize it into
      one canonical file, threading only the short path into the run. This dodges the
      native-engine args-size truncation that silently drops a large inline diff. Each
      consuming microskill (summarize-diff, review-dimension, verify-finding) Reads the path.
  context:
    type: string
    required: false
    description: Optional free-text grounding material (PR/change description, spec excerpt) forwarded to summarize-diff to ground the intent overview.

# Advisory only — documents the reused microskills; no runtime effect (no workflow: nodes here).
imports:
  - microskills/summarize-diff
  - microskills/review-dimension
  - microskills/collect-findings
  - microskills/verify-finding
  - microskills/synthesize-review

# This is a REUSABLE COMPONENT: it produces the review and ENDS at `synthesize`. The
# terminal human action (decide what to do with the report, post it to a PR/MR) is a
# SEPARATE concern owned by whatever workflow incorporates this one — see the "caller owns
# the terminal decision" skeleton in DAG-RULES.md §14. Invoked on its own, the dispatcher's
# Finish step presents the synthesized report to the user.
#
# Compiles to ONE autonomous background segment, ZERO checkpoints (summarize -> 3 sibling
# dimension reviews -> collect -> verify fan-out -> synthesize): no gate, no orchestrator
# node, no loop, so it composes cleanly when nested. The 3 dimension reviews all depend only
# on `summarize`, so they share a dependency rank and the compiler emits them as ONE
# concurrent parallel([...]) batch — they run at the same time, not serially (see
# DAG-RULES.md §12, Intra-segment parallelism). They share `phase_group: review`, so they
# render under one "review" group in the /workflows window.
nodes:
  # Seg 1: summarize the diff into a structured, file-grouped summary that grounds every
  # downstream review. Whole summary object is reused as change_summary by each dimension
  # review and by the synthesis.
  - id: summarize
    use: summarize-diff
    depends_on: []
    inputs:
      diff_path: ${workflow.inputs.diff_path}
      context: ${workflow.inputs.context}
    output_schema:
      type: object
      required: [summary, files, risk_surface, stats]
      properties:
        summary: { type: string }
        files: { type: array }
        risk_surface: { type: string }
        stats: { type: object }

  # Seg 1: correctness review. SIBLING node (not a for_each) because customize.profile is a
  # static literal, not a per-item variable — yet sibling reviews still run CONCURRENTLY:
  # all three share a dependency rank and compile into one parallel([...]) batch. The base
  # profile IS the correctness profile — review-dimension/profiles/base.yaml ships
  # dimension=correctness plus the correctness rubric.
  - id: review_correctness
    use: review-dimension
    customize: { profile: base }
    phase_group: review
    depends_on: [summarize]
    inputs:
      diff_path: ${workflow.inputs.diff_path}
      change_summary: ${summarize.output}

  # Seg 1: security review. SIBLING node applying the security profile overlay
  # (dimension=security + security rubric, opus model).
  - id: review_security
    use: review-dimension
    customize: { profile: security }
    phase_group: review
    depends_on: [summarize]
    inputs:
      diff_path: ${workflow.inputs.diff_path}
      change_summary: ${summarize.output}

  # Seg 1: performance review. SIBLING node applying the performance profile overlay
  # (dimension=performance + performance rubric).
  - id: review_performance
    use: review-dimension
    customize: { profile: performance }
    phase_group: review
    depends_on: [summarize]
    inputs:
      diff_path: ${workflow.inputs.diff_path}
      change_summary: ${summarize.output}

  # Seg 1: fan the three sibling dimension results into one combined, dimension-stamped,
  # globally-identified, deduplicated list. Each dimension node's whole {dimension, findings}
  # output feeds the matching collect-findings input; style is omitted (optional).
  - id: collect
    use: collect-findings
    depends_on: [review_correctness, review_security, review_performance]
    inputs:
      correctness: ${review_correctness.output}
      security: ${review_security.output}
      performance: ${review_performance.output}
    output_schema:
      type: object
      required: [findings, count]
      properties:
        findings: { type: array }
        count: { type: integer }

  # Seg 1: adversarially verify each collected finding against the diff. Fan-out via for_each
  # over the collected findings, bounded by max_parallel; lands in a background segment so the
  # batch cap is honored. The per-item verdicts are consumed downstream as the whole array via
  # ${verify.items}.
  - id: verify
    use: verify-finding
    for_each: ${collect.output.findings}
    as: finding
    max_parallel: 4
    depends_on: [collect]
    inputs:
      finding: ${finding}
      diff_path: ${workflow.inputs.diff_path}

  # Seg 1: synthesize the verified findings into one prioritized report. Consumes the
  # summarize-diff summary, the collected findings, and the per-finding verify-finding verdicts
  # (the whole fan-out array via ${verify.items}). This node's result is the workflow output.
  - id: synthesize
    use: synthesize-review
    depends_on: [summarize, collect, verify]
    inputs:
      change_summary: ${summarize.output}
      findings: ${collect.output.findings}
      verifications: ${verify.items}
    output_schema:
      type: object
      required: [verdict, blocker_count, summary, report_markdown, findings]
      properties:
        verdict: { type: string }
        blocker_count: { type: integer }
        summary: { type: string }
        report_markdown: { type: string }
        findings: { type: array }

# The workflow output IS the synthesized review. Its verdict / blocker_count / report_markdown /
# findings form the output contract a caller consumes — the caller decides whether to post it,
# loop on it, or present it. This component never posts or pauses for a human itself.
output:
  from: synthesize
