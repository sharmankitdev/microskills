version: 1

# UX-heuristic fan-in overlay for collect-findings (MULTIAGENTIC-DESIGN §3.2,
# minted by the ux-design build). Registers the input names ux-design's
# three-dimension heuristic panel wires (review-dimension profiles
# ux-journey-coverage / ux-requirements-fidelity / ux-flow-consistency). Base's
# six code-review inputs are NOT renamed — they deep-merge in, stay optional,
# and go unsupplied. Each entry is an empty map: optional with a null default
# needs no behavioral keys; declaring the name registers it for the caller.
inputs:
  ux_journey_coverage: {}
  ux_requirements_fidelity: {}
  ux_flow_consistency: {}
