version: 1

# Workflow-spec critique fan-in overlay for collect-findings (§8 step 6 / D3). Registers
# the 3 spec review-dimension input names the create-spec-workflow critique panel fans
# over (review-dimension profiles spec-wf-*). Base's six code-review inputs deep-merge in
# unused. Each entry is an empty map. Names = the critique expand item names with
# hyphen→underscore (the inputs_each desugar contract).
inputs:
  spec_wf_coverage: {}
  spec_wf_controlflow: {}
  spec_wf_dataflow: {}
