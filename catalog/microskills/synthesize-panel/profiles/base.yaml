version: 1

inputs:
  candidates:
    required: true
  judge_1: {}
  judge_2: {}
  judge_3: {}
  judges_path:
    required: true

# The body Reads candidate/judge content and Writes the scorecard set at judges_path. Pinning the tool surface
# (matching every sibling file-touching microskill).
runtime:
  allowed_tools: [Read, Write]

output_schema:
  type: object
  required:
    - winner_label
    - winner_path
    - graft_notes
    - candidates_considered
    - judges_heard
    - judges_path
    - summary
  properties:
    winner_label: { type: string }
    winner_path: { type: string }
    graft_notes: { type: string }
    candidates_considered: { type: integer }
    judges_heard: { type: integer }
    judges_path: { type: string }
    summary: { type: string }
