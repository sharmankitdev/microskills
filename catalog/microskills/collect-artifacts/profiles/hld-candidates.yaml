version: 1

# hld-candidates overlay for collect-artifacts (minted per MULTIAGENTIC-DESIGN §3.2).
# Registers the inputs_each-desugared input names for the three HLD candidates the technical-design panel generates (one per stance).
# Each entry is optional with a null default (a skipped candidate rides as null
# and is dropped by the fan-in), so the entries carry no behavioral keys — they
# exist to register the names. Everything else inherits from base.
inputs:
  simplicity_first: {}
  scale_first: {}
  delivery_first: {}
