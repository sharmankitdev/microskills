version: 1

# ux-candidates overlay for collect-artifacts (minted per MULTIAGENTIC-DESIGN §3.2).
# Registers the inputs_each-desugared input names for the three wireframe candidates the ux-design panel generates (one per stance).
# Each entry is optional with a null default (a skipped candidate rides as null
# and is dropped by the fan-in), so the entries carry no behavioral keys — they
# exist to register the names. Everything else inherits from base.
inputs:
  task_flow: {}
  hub_dashboard: {}
  progressive_wizard: {}
