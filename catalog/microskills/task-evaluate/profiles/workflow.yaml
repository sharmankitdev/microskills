version: 1

# Workflow-creation domain: run as the workflow evaluator against its contract
# (it runs validate-workflow + compile-workflow per its own mandate). The verdict
# output_schema (pass, issues) is shared with the microskill domain → inherited from base.
vars:
  contract_doc: .claude/workflow-defs/workflow-create/references/evaluator.md

runtime:
  agent: workflow-evaluator
  # model inherited from base (opus) via deep-merge.
